@model CodingRound.Models.User

@* @{
    ViewData["Title"] = "Login";
} *@

@* <div class="row-md-4">
    <div class="col-md-5">
        <form asp-action="Login">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group text-center"> <h2> Welcome to Relection App</h2></div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
               <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div> <br />
            <div class="form-group">
                <label asp-for="PasswordHash" class="control-label">Password</label>
               <input asp-for="PasswordHash" type="password" class="form-control" />
                <span asp-validation-for="PasswordHash" class="text-danger"></span>
            </div> <br /> <br />
            <div class="form-group">
                <input type="submit" style="width:400px text-center" value="Login" class="btn btn-primary" /> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                <a asp-action="Create" class="btn btn-outline-info text-center">New User ? Register here</a>
            </div> 
        </form>
    </div>
</div> *@

@* @{
     ViewBag.Title = "Login";
     Layout = null;
     <!-- Optional: To exclude layout and focus on the form -->
 } 

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(to bottom, #2b5876, #4e4376);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
        }

        .login-container {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            width: 300px;
            text-align: center;
        }

            .login-container img {
                width: 80px;
                height: 80px;
                border-radius: 50%;
                margin-bottom: 20px;
            }

            .login-container input[type="text"],
            .login-container input[type="password"] {
                width: 100%;
                padding: 10px;
                margin: 10px 0;
                border: none;
                border-radius: 5px;
                font-size: 16px;
            }

            .login-container button {
                width: 100%;
                padding: 10px;
                background: #4CAF50;
                border: none;
                border-radius: 5px;
                color: white;
                font-size: 18px;
                cursor: pointer;
            }

                .login-container button:hover {
                    background: #45a049;
                }

            .login-container .options {
                display: flex;
                justify-content: space-between;
                font-size: 14px;
                margin-top: 10px;
            }

                .login-container .options a {
                    color: #add8e6;
                    text-decoration: none;
                }

                .login-container .options input[type="checkbox"] {
                    margin-right: 5px;
                }
    </style>
</head>
<body>
    <div class="login-container">
        <img src="/images/account.png" alt="User Icon" /> <br />
        <h3>Welcome to Reflection App</h3>
        <form asp-action="Login">
            <input type="text" name="Username" asp-for="Email" class="form-control" placeholder="Username" required />
            <input type="password" name="Password" asp-for="PasswordHash" class="form-control" placeholder="Password" required />
            <button type="submit">Login</button> <br />
            <button type="button" asp-action="Create">New User ? Register here</button>
        </form>
    </div>
</body>
</html>
 *@


@* @{
    Layout = null; // Skip the layout for a clean login page design
} *@

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        body {
            margin-top:20px;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(to bottom, #2b5876, #4e4376);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
        }

        .container {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding-top: 50px; /* To ensure space below navbar */
        }

        .login-container {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            width: 400px;
            text-align: center;
        }

            .login-container h2 {
                margin-bottom: 20px;
                font-size: 24px;
            }

        .form-group label {
            display: block;
            text-align: left;
            margin-bottom: 5px;
            color: white;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
        }

        .btn-primary {
            width: 100%;
            padding: 10px;
            background: #4CAF50;
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 18px;
            cursor: pointer;
        }

            .btn-primary:hover {
                background: #45a049;
            }

        .btn-outline-info {
            display: block;
            margin-top: 15px;
            color: white;
            text-decoration: none;
            font-size: 14px;
        }

            .btn-outline-info:hover {
                text-decoration: underline;
            }

        .text-danger {
            font-size: 14px;
            color: #f8d7da !important;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Welcome to Reflection App</h2>
        <form asp-action="Login">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Email" class="control-label">Email</label>
                <input asp-for="Email" class="form-control" placeholder="Enter your email" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PasswordHash" class="control-label">Password</label>
                <input asp-for="PasswordHash" type="password" class="form-control" placeholder="Enter your password" />
                <span asp-validation-for="PasswordHash" class="text-danger"></span>
                <span>@ViewBag.Error </span>
            </div>
            <button type="submit" class="btn-primary">Login</button>
            <a asp-action="Create" class="btn-outline-info">New User? Register here</a>
        </form>
    </div>
</body>
</html>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
